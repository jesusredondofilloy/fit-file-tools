# Symlink this file into /etc/nginx/conf.d/ so the zone is defined in the
# http {} context (required — nginx rejects limit_req_zone inside server {}).
limit_req_zone $binary_remote_addr zone=fittools:10m rate=10r/s;
